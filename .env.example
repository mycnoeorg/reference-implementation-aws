# CNOE Reference Implementation — Environment Variables
# Copy this file to .env and fill in the values.
# .env is gitignored; never commit real credentials.

# --- AWS ---
AWS_PROFILE=hubcnoe
AWS_REGION=us-west-1
AWS_ACCOUNT_ID=025775160945

# --- Cluster / Domain ---
CLUSTER_NAME=cnoe-ref-impl
DOMAIN=pcsilva.people.aws.dev
ROUTE53_HOSTED_ZONE_ID=Z0125447F67JOS1JXD20

# --- GCP (for provider-terraform GCP workspace) ---
GCP_PROJECT=cksexam-482820
GCP_REGION=us-central1

# --- Keycloak admin (for localhost port-forward demo) ---
KEYCLOAK_ADMIN_USER=cnoe-admin
KEYCLOAK_ADMIN_PASSWORD=<REPLACE_ME>

# --- Demo credentials (displayed during demo; rotate after use) ---
DEMO_SSO_USER=user1
DEMO_SSO_PASSWORD=<REPLACE_ME>
ARGOCD_ADMIN_PASSWORD=<REPLACE_ME>
